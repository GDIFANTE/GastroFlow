// tailwind.config.ts - CORRIGIDO PARA V4

import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      // Futuras customizações de tema (cores, fontes, etc.) virão aqui.
      // Por enquanto, pode ficar vazio.
    },
  },
  plugins: [],
};

export default config;